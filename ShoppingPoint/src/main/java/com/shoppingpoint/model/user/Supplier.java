package com.shoppingpoint.model.user;

public class Supplier extends User {
    public Supplier(String username, String email, String password) {
        setUsername(username);
        setEmail(email);
        setPassword(password);
    }
}
